# Anzdrop UIデザインシステム

Anzdropは、登録不要でも安心して使える、エンドツーエンド暗号化のファイル共有サービスです。この文書は、既存の画面を変更・追加するときに守る視覚・操作上の基準を定めます。対象は開発者とAIエージェントです。

最初に「デザイン原則」「色と文字」「共通部品」を読み、画面固有の判断が必要な場合だけ後半を参照してください。実装済みの共通部品と `app/globals.css` が、この文書より優先される一次情報です。

## 目指す印象は「静かな安心感」と「迷わない操作」

利用者は、ファイルを送る・受け取るという短い目的のために訪れます。装飾で注意を引くより、次にすることをすぐ理解でき、暗号化されていることを押しつけずに伝える画面にします。

- **簡潔**: 1画面につき主要操作は原則1つ。説明は操作の近くに短く置く。
- **率直**: 不可逆な操作、保存期間、パスワードを失った場合の制約は、控えめにせず先に伝える。
- **親しみ**: 柔らかい杏色とドロップのシンボルを使う。ただしファイル共有の信頼性を損なう過度な演出はしない。
- **匿名性を尊重**: 不要な個人情報を求める印象を与えない。アカウント画面でもメールアドレスを前提にした文言・UIを追加しない。
- **内容を覗かない**: ファイル名・復号鍵・パスワードを画面外へ送ることを連想させる機能や文言を追加しない。E2EEの説明は正確で、過剰に断言しない。

## 変更時に守る優先順位

1. `components/brand/` の共通部品と `app/globals.css` を再利用する。
2. この文書のトークン、部品、状態表現に従う。
3. 既存の近い画面を参照して余白・文言・レスポンシブ挙動を揃える。
4. 新しい見た目の規則が必要な場合だけ、最小限のトークンまたは共通部品を追加し、この文書も更新する。

ページ固有の場当たり的な色、角丸、ボタン形状を増やさない。既存のルールに収まらない場合は、実装前にデザイン上の理由を明確にする。

## 基本トークン

### 色

基調は白、ほぼ黒、杏色の3色です。グレーは独立した色ではなく、`ink` の透明度で作ります。

| 名前 | 値 | 用途 |
| --- | --- | --- |
| `paper` | `#ffffff` | ページとカードの背景、白文字の基準 |
| `ink` | `#0a0a0a` | 本文、見出し、強い枠線、第二CTA |
| `brand` | `#f15a22` | 主CTA、選択状態、要点、リンク、進行状況 |
| `ink/70`〜`ink/60` | `ink` の透過色 | 補足本文、通常のリンク |
| `ink/50`〜`ink/40` | `ink` の透過色 | ラベル、補助説明、未選択・無効状態 |
| `ink/10`〜`ink/20` | `ink` の透過色 | カード、区切り線、入力欄の通常枠 |
| `ink/[0.02]`〜`ink/[0.06]` | `ink` の透過色 | 控えめな面、ホバー背景 |
| amber | 管理画面だけで使う状態色 | 一時停止など、削除ではないが注意を要する状態 |

`brand` は強い注意喚起にも現在使われているため、1画面内で多用しない。通常状態では、主CTA、選択中のコントロール、重要なリンクのどれかに用途を絞る。管理画面では、一時停止などの要注意状態にamber、CSAMなど緊急性を明確に区別すべき表示に赤を使ってよい。これらの状態色を一般画面へ拡張しない。

### 文字

本文は `Noto Sans JP` を使う。ロゴの `Anzdrop` は `Quicksand` の700ウェイトを使い、本文へ広げない。アカウントID、共有ID、リカバリーコードなどの識別子は `font-mono` とする。

| 種別 | 基準 | 用途 |
| --- | --- | --- |
| ページ見出し | `text-2xl font-black leading-snug`、広い画面では必要に応じて`sm:text-3xl` | ページの目的を一文で示す |
| セクション見出し | `text-lg font-black` | カード・本文内の節 |
| 強い数値 | `text-xl`〜`text-3xl font-black` | 容量、料金、進行状況など比較の核 |
| 本文 | `text-sm leading-relaxed text-ink/70` | 説明文、規約本文 |
| 補助文 | `text-xs text-ink/50` | ラベル、制約、補足 |
| 小さな注記 | `text-[11px] text-ink/40` | 価格の補足、二次情報 |

見出しは「概要」ではなく、利用者が知りたい結論を表す。例: 「料金プラン」ではなく機能比較を主眼にする画面なら「ファイルの大きさと保存期間で選べる3つのプラン」とする。ボタンは動詞から始め、曖昧な「送信」「実行」を避ける。

### 余白・幅・形

- ページの左右余白は `px-4` を基本とし、広い画面では `sm:px-8` にする。
- コンテンツ幅は目的に合わせる。フォームは `max-w-md`、規約・説明は `max-w-2xl`、複数カードは `max-w-5xl`、共通フッターは `max-w-4xl` を基準にする。
- カードの内側余白は `p-6`、広い画面では `sm:p-8`。ヒーロー的な導入カードは `p-8 sm:p-10` まで使える。
- 大きなまとまりは `space-y-6`〜`space-y-8`、フォーム項目は `space-y-4`、ラベルと入力欄は `space-y-1` を基本にする。
- 角丸は `rounded` または `rounded-lg` を基本にする。モーダルと分析用データカードでは `rounded-xl` まで使える。ピル型は進捗バー・スクロールバーなど、形に意味がある箇所だけに使う。
- 通常カードは `border border-ink/10`。操作対象・入力・選択肢には視認性を優先して `border-2` を使う。

影は例外的に使う。モバイルメニュー、モーダルなど、他の層の上に重なるものだけ `shadow-lg` を使う。通常のカードを浮かせない。

## 共通レイアウトとブランド要素

### サイトの枠

公開画面は、原則として `SiteHeader`、ページ本文、`SiteFooter` の順に置く。ヘッダーは高さ `h-16`、白背景、細い下線、スクロールしても上部に留まる構成です。フッターは細い上線、法務・サポートへの導線、薄いドロップマークを持ちます。

画面本文は、少なくとも `min-h-[calc(100svh-4rem)]` を確保し、短いページでもフッターが不自然に浮かないようにする。ログイン、入力、ダウンロードのように単一操作へ集中させる画面は縦横中央に寄せる。説明・一覧・料金画面は上から読む通常の流れにする。

### ロゴとドロップ

`BrandHeader` はドロップマーク、Quicksandのサービス名、控えめな落下アニメーションをまとめたブランド表現です。ロゴを自作せず、この部品を使う。

`DropMark` は単体でも使える。部品自体が支援技術から隠れるため、大きな装飾として置く場合は `text-brand/10` 以下と `pointer-events-none` を付ける。画面の意味を補わない飾りを増やしすぎない。

### レスポンシブ

- 狭い画面を起点に組み、必要なときだけ `sm`、`md` 以上で拡張する。
- ヘッダーの主要ナビゲーションは `md` 未満ではメニューに収める。情報を隠すのでなく、縦に並べて到達可能にする。
- 横並びの比較カードは `md:grid-cols-*` で段階的に列を増やす。
- 固定幅で横スクロールを発生させない。ID・URL・リカバリーコードは `break-all` と等幅フォントを使う。
- タップ対象は、文字だけの補助リンクを除き、十分な余白を持たせる。アイコンのみのボタンには必ず `aria-label` を付ける。

## コンポーネントの規則

### ボタンとリンク

主CTAは杏色の塗り、白文字、太字です。標準は `rounded bg-brand px-4 py-3.5 text-sm font-black tracking-wider text-paper`。ホバーは `bg-brand/90`、無効状態は `disabled:opacity-30` を使う。

第二CTAは白背景に黒の2px枠を使う。破壊的でない補助操作、戻る、再読み込み、プランの比較開始などに用いる。ホバーでは `bg-ink/[0.03]` または枠線を濃くする。

テキストリンクは `text-brand` または `text-ink/60` を基準にし、ホバーとキーボード操作時に下線または色変化で操作可能だと分かるようにする。リンクとボタンを見た目だけで使い分けない。画面遷移にはリンク、現在の画面の状態を変える操作にはボタンを使う。

### 入力欄と選択肢

入力欄は、通常時 `border-2 border-ink/20`、フォーカス時 `focus:border-brand`、角丸、左右 `px-3`、縦 `py-2` を基準にする。小さな画面では `text-base` を使ってOSの自動拡大を防ぎ、`sm` 以上で必要なら `text-sm` にする。

ラベルは入力欄の上に置き、`text-xs font-bold text-ink/50` とする。必須・任意、文字数、復旧できない制約など、判断に必要な情報はラベルの近くに置く。プレースホルダーだけで項目の意味を伝えない。

密度の高い選択肢（保存期間など）は、選択中だけ杏色の塗りと白文字にする。プランカードのように比較しながら選ぶ選択肢は、杏色の枠とごく薄い杏色の背景で選択を示し、黒文字を保つ。未選択は黒系の枠と白背景に留める。パスワード入力には `PasswordInput` を使い、表示切替のアクセシブルなラベルを維持する。

### カード、通知、空状態

カードは情報を囲むためのもので、同じ画面で無意味に分割しない。通常カードは白背景と細線、選択・推奨プラン・成功後の共有URLのように強調すべき対象だけ杏色の2px枠を使う。

- **通常情報**: `border-ink/10` と黒の透過色で静かに見せる。
- **選択・推奨**: `border-brand`。塗りで埋めず、白地を保つ。
- **エラー・要対応**: `text-brand` と必要なら `border-brand`。原因と次の行動を短く書く。
- **無効・準備中**: `ink/30`〜`ink/40`。クリックできないことを見た目と `disabled` の両方で示す。
- **空状態**: イラストの代わりに、短い説明、次に行う操作、必要なら枠線付きの領域を使う。

エラーを赤い全面バナーやトーストで乱発しない。フォーム内なら該当項目の近く、画面全体の失敗なら主操作の近くに表示する。技術的な例外文・内部ID・復号鍵を表示しない。

### モーダル、ローディング、進捗

モーダルはQRコード表示、重要な確認など、現在の画面を離れずに完結すべき操作だけに使う。背景は内容を隠せる `ink` 系の半透明オーバーレイにする。新しいモーダル、または既存モーダルを変更する際は、Esc、閉じるボタン、`role="dialog"`、`aria-modal`、初期フォーカスとフォーカストラップを実装する。

ローディングには `Spinner` を使い、読み込み中であることを `role="status"` とラベルで伝える。アップロードは、短時間の待機だけで済ませず、進捗率と杏色の細い進捗バーを表示する。アニメーションは情報を補助するものに限り、`prefers-reduced-motion` を尊重する。

## 画面別の判断

### アップロード画面は、ファイル選択を主役にする

`/` では、ファイル選択・ドラッグ&ドロップ領域を最初に目に入る大きな操作対象にする。状態は「未選択」「選択済み」「アップロード中」「共有URL発行済み」を明確に分け、同じ領域を使って状態遷移を見せる。

保存期間、パスワード保護などの詳細設定は、ファイル選択を妨げない位置に置く。暗号化やパスワードの説明は、必要な時点だけ短く示し、専門用語を連続させない。共有URLと復号鍵の扱いは、コピー・QR・共有といった次の行動を主CTAより弱い操作として並べる。

### ダウンロード画面は、復号と保存の状態を隠さない

`/d/[shareId]` では、パスワードの要否、ファイル名の復号状況、ダウンロード中のファイルを示す。利用者が待っている理由を曖昧にしない。進捗を正確に測定できる処理では、進捗率または進捗バーを併せて示す。ファイル名や容量は識別しやすく、長い名前でもレイアウトを壊さない形にする。

### アカウント・決済画面は、慎重さを先に伝える

`/mypage/**` と `/pricing` は、入力フォームを `max-w-md` 程度に絞る。メールを使わないこと、リカバリーコードを失うと復旧できないこと、自動更新・解約予約・有効期限を、購入や設定変更の前後に明示する。

料金比較では、共通項目を揃えたカードを使う。Premiumのように推奨する選択肢は杏色の枠と要点の杏色で示すが、過度なバッジや割引演出は使わない。利用できないプランは押せるように見せない。

### 説明・法務・サポート画面は、読む負担を減らす

`/about`、`/legal/**`、`/contact`、`/report/**` では、本文幅を絞り、見出しと段落の余白で読み進められるようにする。法務ページの導入は淡い杏色のグラデーションと薄いドロップマークでまとめ、本文カードは白地と細線に留める。

通報・問い合わせでは、感情的または断定的な文言を避ける。送信前に何を入力するか、送信後に何が起こるかを明記する。通報理由などの自由記述欄で、復号鍵を貼らないよう必要に応じて注意を添える。

### ブログは、読み物の余白と一覧性を両立する

`/blog/**` は、記事一覧では `max-w-6xl` の広い領域にカードを並べ、画面幅に応じて2列、3列へ増やす。記事本文は `max-w-3xl` に絞り、本文を読むリズムを優先する。カードの画像は `aspect-video` を保ち、ホバー時の拡大は控えめにして、画像だけでなく記事タイトルからも遷移できるようにする。

### 管理画面は、誤操作より判断しやすさを優先する

`/admin/**` は公開画面と同じ色・文字の基盤を保ちつつ、一覧、フィルター、状態、操作を明確に分ける。削除など不可逆な操作には確認を挟み、停止・再開・対応済みの状態はラベルとボタン文言の両方で区別する。緊急性の高いCSAM通報だけは赤で明確に分離する。

## アクセシビリティと安全性は見た目より優先する

- すべての操作はキーボードで到達・実行できるようにする。`focus-visible` では杏色のアウトラインを消さない。
- 色だけで状態を表さない。選択・エラー・準備中には、文言、アイコン、枠線などを組み合わせる。
- 本文・補助文・ボタン文字は、背景とのコントラストを確保する。薄い文字を本文や主要操作へ使わない。
- フォームの `label`、エラーの `role="alert"`、ローディングの `role="status"`、アイコンボタンの `aria-label` を省略しない。
- モーダルや折りたたみメニューでは、開閉状態を支援技術へ伝え、閉じた内容をフォーカス可能なまま残さない。
- 秘密情報を扱う画面では、復号鍵・パスワード・リカバリーコードをログやエラー表示に含めない。コピー操作には成功・失敗を明示する。

## やらないこと

- グラデーション、影、ガラス風表現を通常カードやCTAへ増やさない。
- 青・紫などの新しいブランド色、独自のグレー、独自の角丸をページ単位で追加しない。
- 「今すぐ」「無料」「安全」を必要以上に繰り返す販売的な文言にしない。
- 操作のたびにモーダル、トースト、アニメーションを出さない。
- 主要な行動をアイコンだけにしない。アイコンは文字と併用するか、アクセシブルな名前を付ける。
- デスクトップだけを基準に固定幅のUIを作らない。
- E2EEを理由に、利用者が保存期間・パスワード紛失・共有リンク漏えいのリスクを見落とすUIにしない。

## 実装時の確認

画面を追加・変更したら、次を確認する。

- 既存の `components/brand/` を再利用できているか。
- 主CTAが1つに絞られ、画面の目的が見出しと最初の画面領域で分かるか。
- 狭い画面と広い画面の両方で、操作・長い文字列・エラー表示が崩れないか。
- 通常、ホバー、フォーカス、無効、送信中、成功、失敗の状態を必要な範囲で実装したか。
- キーボード操作、ラベル、フォーカス表示、`prefers-reduced-motion` を損なっていないか。
- E2EE、パスワード、リカバリーコードに関する文言が、実装と矛盾せず、秘密情報を露出しないか。

この文書で判断できない場合は、近い既存画面と `components/brand/` を優先し、それでも新しい規則が必要なら、実装と同じ変更でここへ理由と規則を追記する。
