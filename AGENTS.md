<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## 基本ルール

- 回答は基本的に日本語で行うこと。
- 変更を加えた場合は、可能な限り関連するテストを追加すること。
- 変更を加えた場合は、可能な範囲で既存のテストも実行し、変更による影響がないことを確認すること。
- コアとなる部分が E2E（エンドツーエンド）暗号化であるため、できるだけサーバー側に情報を持たない設計を重視すること。
- サーバー側に新しい情報を保存する必要がある場合は、実装前に必ずユーザーの許可を求めること。ユーザーの明示的な許可なしに、D1 などへ新しい永続データを保存する設計・実装を追加してはいけない。
- 認証情報、API キー、`.env` などの秘密情報をコミットしないこと。
- 大規模な変更を行う前には、関連するコードだけでなく、必要に応じてプロジェクト全体を調査し、既存の設計や実装への影響を確認すること。
- できる限り保守しやすく、シンプルで理解しやすいコードを書くこと。
- 既存の実装・設計・命名規則がある場合は、それを尊重すること。
- 不要な依存関係や複雑な仕組みを安易に追加しないこと。
- 要件や設計上の判断が必要な事項について、推測で仕様を決めないこと。特にセキュリティ、暗号化、データ保存、認証・認可に関わる判断は、必要に応じてユーザーに確認すること。

## セキュリティ・プライバシー

このプロジェクトでは E2E 暗号化を重要な設計原則とする。

- サーバーは可能な限りユーザーの機密情報や復号可能な情報を保持しないこと。
- クライアント側で完結できる処理を、安易にサーバー側へ移さないこと。
- 新たにサーバー側へ情報を保存する必要がある場合、その情報の種類、保存理由、保存期間、暗号化の有無、および E2E 暗号化への影響を確認すること。
- 新規の永続化が必要な場合は、実装前にユーザーへ確認し、明示的な許可を得ること。
- 秘密情報や認証情報をログ、エラーメッセージ、レスポンスなどに不用意に含めないこと。
- `.env`、API キー、秘密鍵、アクセストークンなどをソースコードやコミットに含めないこと。

## データ保存に関する重要事項

**このルールは特に重要。**

D1 を含むサーバー側の永続ストレージに、新しい種類の情報を保存する変更を行う場合は、ユーザーの明示的な許可が必要。

ユーザーの許可なしに以下を行ってはいけない。

- 新しい D1 テーブルを追加する
- 既存テーブルに新しい永続データを追加する
- 新しいユーザー情報をサーバーに保存する
- クライアントで保持していた情報をサーバーへ移す
- ログやキャッシュなどを利用して、実質的に新しいユーザー情報を永続化する

既存のデータを利用した読み取り・更新などについても、E2E 暗号化やプライバシーへの影響がある場合は、実装前に確認すること。

## 技術スタック

- 言語: TypeScript
- フレームワーク: Next.js
- パッケージマネージャー: npm
- データベース: SQLite / Cloudflare D1
- テスティングフレームワーク: Vitest

## 開発方針

### コード

- TypeScript の型安全性を活用し、可能な限り `any` に頼らないこと。
- 既存のコードスタイルやプロジェクトの設定に従うこと。
- 同じ処理を複数箇所に重複させず、適切に共通化すること。
- ただし、過度な抽象化は避け、コードの理解しやすさを優先すること。
- 将来の変更や保守を考慮し、責務を明確にすること。
- 既存コードで解決できる問題に対して、新しいライブラリや複雑な仕組みを安易に導入しないこと。

### テスト

- 機能変更やバグ修正を行った場合は、可能な限り Vitest のテストを追加・更新すること。
- バグ修正では、可能であれば同じ問題が再発しないことを保証するテストを追加すること。
- テストが存在する既存機能を変更する場合は、関連するテストも確認すること。
- テストを実行できなかった場合は、その理由を報告すること。

### ドキュメント

- 新機能を実装した場合、必ず関連するドキュメントを更新すること。
- 既存の設計を変更した場合、必ず関連するドキュメントを更新すること。
- 実装とドキュメントの内容が乖離しないようにすること。
- 既存のドキュメントがある場合は、まずそれを確認し、適切な場所へ追記・更新すること。
- 対応するドキュメントが存在しない場合は、プロジェクトの既存のドキュメント構成に合わせて追加すること。
- ドキュメントの更新を忘れないよう、実装完了時に確認すること。

### 変更の進め方

大規模な変更を行う場合は、いきなり実装を開始せず、まず以下を確認すること。

1. 関連するディレクトリ・ファイルを調査する。
2. 必要に応じてプロジェクト全体を調査する。
3. 現在のアーキテクチャとデータフローを把握する。
4. 既存の実装や類似機能を確認する。
5. E2E 暗号化やデータプライバシーへの影響を確認する。
6. 必要に応じて変更方針をユーザーに確認する。
7. 方針が決まったら、必要最小限の変更を行う。
8. 関連するドキュメントを更新する。
9. テスト・型チェック・Lint など、利用可能な検証を実行する。
10. 実装後の検証エージェントによるレビューを行う。
11. 指摘事項を確認し、妥当なものを修正する。
12. 必要に応じて修正後のテスト・検証を再度実行する。

## 実装後の検証

ユーザーに変更内容を提出する前に、必ず別のエージェントを起動して変更内容を検証すること。

検証エージェントには、実装者とは独立した視点でレビューさせること。

### テストを追加・変更した場合

以下の観点で検証させること。

- 楽観的すぎるテストになっていないか。
- 実装の詳細ではなく、本来保証すべき振る舞いを検証できているか。
- 正常系だけでなく、境界値・異常系・失敗ケースが適切に考慮されているか。
- テストが実装そのものをなぞるだけのものになっていないか。
- テストが実際のバグを検出できる十分な内容になっているか。

### 新機能を実装した場合

以下の観点で検証させること。

- セキュリティ上の脆弱性がないか。
- E2E 暗号化やプライバシーを損なっていないか。
- 認証・認可に問題がないか。
- ユーザー入力を安全に扱えているか。
- 意図しない情報漏洩が発生しないか。
- 意図しないサーバー側へのデータ保存が発生していないか。
- 既存機能や既存のセキュリティモデルを壊していないか。
- エラー処理や境界条件に問題がないか。

### 検証結果への対応

- 検証エージェントから指摘を受けた場合は、内容を確認すること。
- 指摘が妥当だと判断した場合は、必ず修正すること。
- 修正した場合は、必要に応じて再度テスト・検証を行うこと。
- 検証エージェントの指摘をすべて無条件に採用する必要はないが、採用しなかった場合は、その理由を説明できる状態にすること。
- 検証を省略してユーザーへ提出してはいけない。

## デプロイ

GitHub Actions を利用して Cloudflare Workers へ自動デプロイする構成になっている。

- `main` ブランチの更新(PR のマージ)によって自動的にデプロイされる。
- `main` はブランチ保護がかかっており、直接 push はできない(GitHub 側で拒否される)。反映するには必ず PR を経由する。
- PR は CI(lint・型チェック・テスト)がグリーンでないとマージできない設定になっている。
- ユーザーから明示的に指示されていない限り、不要なブランチ操作やリリース作業を行わないこと。

## Git

- 新機能の開発・リファクタリング・バグ修正など、コードに変更を加える場合は、基本的に `main` から作業用ブランチ(topic branch)を切って作業し、`main` へは PR 経由で反映する。`main` への直接コミット・直接 push は行わない。
- ユーザーの明示的な指示なしにコミットしないこと。
- ユーザーの明示的な指示なしに、作業用ブランチの push・PR の作成をしないこと。「push して」「PR を出して」等の指示は、作業用ブランチの push と PR の作成までを意味し、マージまでは含まない。
- ユーザーの明示的な指示なしに PR をマージしないこと。マージは「マージして」等、別途明示的な指示を受けてから行う。
- PR のマージ方式は Squash merge を使う(リポジトリ設定で Squash merge 以外は無効化済み)。マージ後、作業用ブランチは自動的に削除される(`delete_branch_on_merge`)。
- 秘密情報や認証情報をコミットしないこと。
- コミット・push・PR 作成の前に変更内容を確認すること。
- コミットメッセージに、Claude Code・AI・生成 AI などがコーディングしたことを示す文言を含めないこと。
- コミットメッセージは、変更内容や目的が分かる簡潔で自然なメッセージにすること。
